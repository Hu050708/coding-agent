# 两分钟视频方案

目标时长约 110 秒；所有演示命令从仓库的 `backend/` 目录运行。录制前关闭通知，
在当前终端进程中预先注入 `DEEPSEEK_API_KEY`，但不要在命令、输出、`.env`、浏览器密钥页
或个人目录中显示它。

## 画面与讲解

1. **0–12 秒：任务与边界**
   - 显示题目一句话和 Coding Agent README 标题。
   - 讲：从零实现 coding-agent 循环，只用厂商底层 API 客户端，不用 Agent 框架或服务端工具。

2. **12–30 秒：架构**
   - 显示 `src/coding_agent` 目录和 README 架构图。
   - 讲：core 维护历史、预算和终止；provider 只适配 DeepSeek；tools/security 本地校验执行；diagnostics 只写脱敏元数据。

3. **30–78 秒：真实任务闭环（可加速）**
   - 从全新 `examples/date_boundary_bug` 基线运行 `scripts/run_demo_trial.py`。
   - 展示模型依次列文件、读实现、带 hash 修改、运行 pytest、返回总结。
   - 终端只展示模型/工具序号、工具名、状态和耗时，不展示 reasoning、参数正文或文件内容。

4. **78–95 秒：独立验收**
   - 展示 verifier 的四项结果：候选测试、新增回归测试、日末边界、原行为。
   - 强调 verifier 在 agent 工作区外生成隐藏数据，并从 CLI 黑盒执行。

5. **95–110 秒：可靠性与限制**
   - 显示本次全量离线测试与独立 verifier 结果；只有在录制前确实复跑并通过时，才展示 live smoke 结果。
   - 讲清 `shell=False` 不是 OS 沙箱、DeepSeek Flash 是滚动模型别名、结果由独立测试而非模型自述判定。

## 当前可复核基线（2026-08-27）

- 一次全新副本正式 demo 已真实通过：`deepseek-v4-flash`，14 次模型调用、27 次工具调用、37.9 秒。
- 外部 verifier 四项全部通过；结果来自独立黑盒执行，不采用模型自述作为通过依据。
- 该记录只代表一次成功样本；下面的连续三次录制门槛尚需在正式录制前重新执行。

## 录制前检查

- 正式 demo 连续成功三次；使用其中一次全新副本录制。
- MP4 小于 2 分钟、200 MB；检查声音、字幕、加速段仍可辨认。
- 逐帧确认无 key、`.env` 内容、账户信息或推理内容。
- ZIP 根层只放最终 `README.txt` 与 MP4，不嵌套目录。
