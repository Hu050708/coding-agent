项目：Coding Agent 编程智能体
仓库地址：<项目完成并公开后填写 GitHub URL>

环境：Python 3.11、Conda、Docker Desktop、Node.js 22。仓库根目录名为 coding-agent。进入 backend，执行“conda env create -f environment.yml”和“conda activate coding-agent”，按 .env.example 在未入库的 .env 中配置 DEEPSEEK_API_KEY、允许根目录和 PostgreSQL 密码/URL。

Web 运行：先在 backend 执行“docker compose --env-file .env -f deploy/compose.yml up -d”，启动独立的 coding-agent-postgres（127.0.0.1:5434）和数据卷；再执行“coding-agent-web”。另一终端进入 frontend，执行“npm ci”和“npm run dev”，访问 http://127.0.0.1:5173/。CLI 可脱离 Docker、数据库和 Web 运行：coding-agent --workspace <项目目录> "修复问题、补测试并验证"。

特色：不使用 Agent 框架或 Agent SDK，自行实现 DeepSeek tool calling 的历史、解析、本地工具、循环终止、重试和预算。Web 支持受限工作区、持久会话、SSE 断线重放、审批、手动确认的长期记忆，以及 review/workspace/auto 三档后端权限；DENY 始终不能绕过。PostgreSQL 只保存可见消息和安全事件，不保存隐藏推理或完整工具输出。

边界：工作区和命令策略不是 OS 沙箱；获准程序仍有当前用户权限。开发中使用 Codex/ChatGPT 辅助分析、实现与审查，作者理解并验证全部设计，对项目负责。
