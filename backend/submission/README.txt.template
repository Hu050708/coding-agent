项目：Coding Agent 编程智能体
仓库地址：<公开 GitHub URL>

运行：安装 Docker Desktop，在仓库根目录将 .env.docker.example 复制为 .env，填写 DEEPSEEK_API_KEY、数据库密码和宿主机工作区路径，执行 docker compose up -d --build。浏览器访问 http://127.0.0.1:8080。API 文档位于 http://127.0.0.1:8080/api/docs。

CLI：进入 backend，按 environment.yml 创建并激活 coding-agent 环境，在当前终端设置 DEEPSEEK_API_KEY，然后执行 coding-agent --workspace <项目目录> "修复问题、补测试并验证"。CLI 不依赖 Web 和 PostgreSQL。

特色：项目自行实现对话历史、模型输出解析、Agent 循环、预算、重试、终止和八个本地工具，不使用 Agent 框架或服务端文件工具。文件操作受工作区限制，修改和删除使用 SHA-256 防止覆盖并发变化。Web 支持持久会话、SSE 断线重放、审批、取消、三档权限和用户确认的工作区记忆。正式评测从全新任务副本开始，并由 Agent 工作区外的 verifier 验收。

边界：命令策略不是操作系统沙箱；获准程序仍有当前运行账户权限。开发过程中使用 Codex/ChatGPT 辅助分析、实现和审查，作者负责设计与验证。
